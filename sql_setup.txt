CREATE TABLE emotes (id VARCHAR(30) NOT NULL, uses INT NOT NULL, messages INT NOT NULL, reacts INT NOT NULL);
ALTER TABLE emotes ADD UNIQUE(id);
CREATE TABLE users (id VARCHAR(30) NOT NULL, rsent INT NOT NULL, rrecv INT NOT NULL);
ALTER TABLE users ADD UNIQUE(id);
CREATE TABLE settings (server VARCHAR(30) NOT NULL, channel VARCHAR(30) NOT NULL);